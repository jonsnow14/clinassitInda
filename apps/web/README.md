# ClinAssistIndia web UI

Runs on **port 3002** (`npm ci && npm run dev`). Do not use ports 3000 or 3001.

See the repo root README for clone path (`cd clinassitInda`), API on **8002**, and ICMR ingest. Use `npm ci` (lockfile is in git), not `npm install`.

# Chroma Vector Database

This directory holds the generated ChromaDB SQLite database for vector retrieval.

Run the ingest pipeline to populate it from the ICMR PDFs:
```bash
cd apps/api
python -m app.rag.ingest
```

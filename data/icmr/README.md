# ICMR Standard Treatment Workflows

This directory is the destination for downloaded ICMR STW PDF files.

Run the ingest script to populate this directory:
```bash
cd apps/api
python -m app.rag.ingest
```
